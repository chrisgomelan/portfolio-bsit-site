[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA redirect — sends all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
